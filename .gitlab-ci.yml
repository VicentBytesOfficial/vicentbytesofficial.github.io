pages:
  image: alpine:latest
  stage: deploy
  script:
    - mkdir public/
    - mkdir public/es
    - mkdir public/images
    - cp -r es/* public/es
    - cp -r images/* public /images
  artifacts:
    paths:
      - public
  only:
    - main